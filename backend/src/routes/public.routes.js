import { Router } from "express";

const router = Router();

/*
    Future Routes

    GET /home

    GET /about

    GET /timeline

    GET /gallery

    GET /faqs

    GET /coordinators

    GET /announcement
*/

export default router;
